// 1. 기본 문법
class User {
    constructor(name) {
        this.name = name;
    }

    sayHi() {
        alert(this.name);
    }
}

let user = new User("John");
user.sayHi();

// 2. 클래스란
// 자바스크립트에서 클래스는 함수의 한 종류입니다.

// 3. 클래스는 단순한 편의 문법이 아닙니다


// 4. 클래스 표현식


// 5. getter와 setter


// 6. 계산된 메서드 이름 […]


// 7. 클래스 필드

